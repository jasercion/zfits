//! Public package entry point for zfits.
//!
//! The implementation remains in `main.zig` for compatibility with the
//! existing source layout; importing this file exposes the complete library
//! API from the package root.
pub usingnamespace @import("main.zig");
